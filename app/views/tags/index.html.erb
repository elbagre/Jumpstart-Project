<h1>All Tags</h1>

<u1 id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag) %>
    </li>
  <% end %>
</u1>

<%= link_to "<< Back to Article Index", articles_path %>